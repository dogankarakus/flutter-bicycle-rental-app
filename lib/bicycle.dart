// ignore_for_file: unused_import, file_names

import 'package:flutter/material.dart';

class Bicycle {
  String name;
  String owner;
  String adress;
  String posterPath;
  double price;
  double rating;
  int ratingCount;

  Bicycle(this.name, this.posterPath, this.price, this.rating, this.owner,
      this.ratingCount, this.adress);
}
